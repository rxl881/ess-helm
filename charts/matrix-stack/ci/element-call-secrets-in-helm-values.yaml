# Copyright 2024-2025 New Vector Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only
#
# source_fragments: element-call-minimal.yaml element-call-secrets-in-helm.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# initSecrets, postgres don't have any required properties to be set and defaults to enabled
elementCall:
  ingress:
    host: rtc.ess.localhost
  livekitKey:
    secret: '{{ $.Release.Name }}-element-call-external'
    secretKey: livekitKey
  livekitSecret:
    secret: '{{ $.Release.Name }}-element-call-external'
    secretKey: livekitSecret
elementWeb:
  enabled: false
matrixAuthenticationService:
  enabled: false
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
